To run tests, you need to install jest by running "npm install jest" in /backend,
then run tests by typing "npm test" in /tests

If you're writing your own tests, make sure the file is formatted as "[filename].test.js" so that jest will recognize it